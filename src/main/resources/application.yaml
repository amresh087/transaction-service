server:
  port: 5057

spring:
  application:
    name: transaction-service

  datasource:
    url: jdbc:mysql://localhost:3306/retail_db
    username: root
    password: ellenverma
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

